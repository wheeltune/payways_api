from django.urls import include, path

from . import views

urlpatterns = [
    # path('<int:pk>/', views.UserView.as_view()),
    # path('<int:pk>/accept', views.UserView.as_view()),
    # path('<int:pk>/deny', views.UserView.as_view()),
]
